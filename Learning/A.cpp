#include <bits/stdc++.h>

#define int int64_t
#define pb push_back
#define sz(x) (int)(x.size())
#define ALL(x) (x).begin(),(x).end()
#define F0R(i,R) for(int i = (0); i < (R); ++i)
#define FOR(i,L,R) for(int i = (L); i <= (R); ++i)

using namespace std;

struct ${ $() { ios::sync_with_stdio(0); cin.tie(0); } } $;

template<class T> bool cmin(T& a, const T& b) { return b < a ? a = b, 1 : 0; }
template<class T> bool cmax(T& a, const T& b) { return a < b ? a = b, 1 : 0; } 

struct school {
	static int nachiket;
};

int school::nachiket = 21;

void test2() {
	int n;
	cin >> n;
	vector<int> a(n);
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
}

vector<int> adj[N];

void test() {
	int n;
	cin >> n;
}

int32_t main() {
	school PICT;
	school AIT;
	school Walchand;
	cout << PICT.nachiket << endl;
	PICT.nachiket = 22;
	cout << AIT.nachiket << endl;
	AIT.nachiket = 69;
	cout << PICT.nachiket << endl;
	cout << AIT.nachiket << endl;
	cout << Walchand.nachiket << endl;
}

