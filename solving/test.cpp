#include <bits/stdc++.h>
using namespace std;

void test() {
	string ret;
	ret.push_back('C');
	reverse(ret.begin(), ret.end());
	const int n = ret.size();
	for (int i = 0; i < n; i++) {
		int x;
		cin >> x;
	}
	for (int i = 0; i < n; i++) {
	}
	print();
	for() {
	:}
}

int hello() {
	set<int> f;
	bool is_empty = f.empty();
	return is_empty;
}

int main() {
	int n;
	cin >> n;
	vector<int> A;
}
