#include <bits/stdc++.h>
using namespace std;

class Solution {
	public:
	int findMinimumTime(vector<vector<int>>& A) {
		const int N = A.size();
		sort(A.begin(), A.end());
		int ans = 0;
		return ans;
	}
};

int main() {
	return 0;
}

